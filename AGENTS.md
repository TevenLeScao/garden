Never commit to git
When debugging visual issues, export the SVG to an image if the SVG is not visual enough
Always test the code before returning by running: uv run python sketch_claude_garden.py save
